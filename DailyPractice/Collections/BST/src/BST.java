public class BST <T>{
    Node<T> root;
    void insertData(T t){
        Node<T> newNode=new Node<>();
        newNode.setData(t);
        if(root==null){
            root=newNode;
        }
        else {
            Node<T> nextNode=root;
            
        }
    }
}
